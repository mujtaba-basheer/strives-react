import React from "react";

import Homepage from "../components/Homepage/index";

const Home = (props) => {
  console.log(props);
  return (
    <>
      <Homepage />
    </>
  );
};

export default Home;
